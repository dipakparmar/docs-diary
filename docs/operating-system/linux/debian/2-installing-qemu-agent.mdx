---
id: debian-install-qemu-agent
title: How to install QEMU Agent on Debian/Ubuntu
sidebar_label: Install QEMU Agent on Debian/Ubuntu
description: >
  This article will help you to install QEMU Agent on Debian/Ubuntu.
  QEMU Agent is a QEMU guest agent that can be used to monitor and control
  QEMU virtual machines.
keywords: [install qemu guest agent, install qemu-agent on debain, qemu-agent ]
tags: [QEMU, Virtulization, Debian, Ubuntu, Linux, Operating System]
---


### Install qemu-guest-agent package 

```bash
apt -y install qemu-guest-agent
```

### Enable QEMU guest agent

```bash
systemctl enable qemu-guest-agent
```

### Start QEMU guest agent

```bash
systemctl start qemu-guest-agent
```

### Check QEMU guest agent status running

```bash
systemctl status qemu-guest-agent
```
